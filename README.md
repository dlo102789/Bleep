# React Chat App

To install, type NPM install in the server directory.

To Run, type NPM Run Start in the server directory, then CD to the client folder and type NPM Run Start

Features to be added:
-- Separate chat rooms
-- Persistent User IDs
-- Persistent messages
